#include "main.h"
/**
 * _abs - outputs absolut value of integer input
 * @n : integer to check
 * Return: 0 or -1
 */
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
return (0);
}
