#include "main.h"
/**
 * mul - multiplication of two numbers
 * @a : firts variable to multiply
 * @b : second variable to multiply
 * Return: return result of multiply
 */
int mul(int a, int b)
{
return (a * b);
}
